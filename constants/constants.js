const RPN_REGEX = /^[0-9\+\-\*\-\/ ]*$/;

const WELCOME = "Welcome";

const GOODBYE = "Goodbye!";

const PROMPT = "> ";

module.exports = Object.freeze({
  RPN_REGEX,
  WELCOME,
  GOODBYE,
  PROMPT,
});
